spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jpashop; #MVCC=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#       show_sql: true system.out 을 통해 표시
        format_sql: true
        default_batch_fetch_size: 100
#    open-in-view: false
logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace